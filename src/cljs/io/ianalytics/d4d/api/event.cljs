(ns io.ianalytics.d4d.api.event
  "Provides functions to CRUD events."
  (:require [io.ianalytics.d4d.api.core :refer [API_URL]]))

(defn groupEventsByID "" [])


(defn getAllEvents [])


(defn createEvent
  []  )

(defn updateEvent
  []  )

(defn getEvent
  []  )

(defn deleteEvent
  []  )

(defn- combineSeverities [s1 s2] s1)

(defn combineTwoEvents [e1 e2]
  e1)

(defn combineEvents [eventsToCombine]
  (first eventsToCombine))
