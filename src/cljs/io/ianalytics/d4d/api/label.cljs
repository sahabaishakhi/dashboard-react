(ns io.ianalytics.d4d.api.label
  "Provides functions to CRUD labels."
  (:require [io.ianalytics.d4d.api.core :refer [API_URL]]))


(defn getAllLabels [])


(defn getLabel [])


(defn createLabel [])


(defn deleteLabel [])
